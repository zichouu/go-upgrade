name: lint
on:
  push:
    branches:
      - main
      - renovate/**
  pull_request:
    branches:
      - main
  workflow_dispatch:
jobs:
  lint:
    uses: zichouu/actions/.github/workflows/go-lint.yml@main
    permissions:
        contents: write
        pull-requests: read
        checks: write
